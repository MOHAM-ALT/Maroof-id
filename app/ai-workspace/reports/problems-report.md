# تقرير المشاكل التقنية - معروف (Maroof ID)
# التاريخ: 2026-02-25

يوضح هذا التقرير الأخطاء والعيوب البرمجية التي تم اكتشافها أثناء الفحص الشامل للمشروع.

---

## 1. أخطاء حرجة (Severity: Critical) 🚨

### 1.1 فقدان البيانات عند تحديث الروابط الاجتماعية
*   **الموقع:**
    *   `app/Http/Controllers/Customer/CardController.php` (دالة `saveSocialLinks`, السطر 241)
    *   `app/Http/Controllers/Customer/CardBuilderController.php` (دالة `save`, السطر 117)
*   **الوصف:** يقوم النظام بحذف جميع الروابط الاجتماعية المرتبطة بالبطاقة (`$card->socialLinks()->delete()`) قبل إعادة إنشائها عند كل عملية حفظ.
*   **الأثر:** يؤدي هذا إلى تصفير عداد النقرات (`clicks_count`) للروابط في كل مرة يقوم فيها المستخدم بتعديل بطاقته، مما يفقد المنصة أهم ميزة إحصائية للمستخدمين.

### 1.2 تعارض خطير في منطق التسعير (Business Logic Error)
*   **الموقع:** `app/Http/Controllers/Public/PricingController.php`
*   **الوصف:** الكود يعرف الخطط كاشتراك "شهري" بينما التسويق في صفحة الهبوط يؤكد أنها "مرة واحدة للأبد".
*   **الأثر:** تضليل العميل وفشل في الوفاء بالوعد التجاري للمنصة.

### 1.3 عدم تطابق نموذج التسجيل مع قاعدة البيانات
*   **الموقع:** `app/Http/Controllers/Auth/RegisterController.php` (دالة `register`)
*   **الوصف:** يقوم المتحكم بالتحقق من حقل `phone` ومحاولة حفظه، ولكن عمود `phone` غير موجود في جدول `users` في قاعدة البيانات.
*   **الأثر:** فشل عملية التسجيل أو تجاهل حفظ رقم الجوال دون تنبيه المستخدم، مما يسبب نقصاً في بيانات العملاء.

---

## 2. أخطاء متوسطة (Severity: Medium) ⚠️

### 2.1 ضعف أمني في حماية البطاقات بكلمة مرور
*   **الموقع:** `app/Models/Card.php` (دالة `checkPassword`, السطر 141)
*   **الوصف:** تتم مقارنة كلمات مرور البطاقات بنصها الصريح باستخدام المشغل `===` بدلاً من استخدام تشفير الهاش.
*   **الأثر:** تخزين كلمات المرور كنص صريح في قاعدة البيانات يمثل خطراً أمنياً، حيث يمكن لأي شخص لديه صلاحية الوصول لقاعدة البيانات رؤية كلمات المرور.

### 2.2 الاعتماد على خدمات خارجية لتوليد رموز QR
*   **الموقع:** `app/Http/Controllers/Customer/CardController.php` (دالة `downloadQR`)
*   **الوصف:** يتم استدعاء API خارجي (`api.qrserver.com`) لتوليد رموز QR رغم وجود مكتبة `endroid/qr-code` وخدمة مخصصة `QrCodeService` داخل المشروع.
*   **الأثر:** بطء في استجابة النظام والاعتماد على طرف ثالث قد يتوقف في أي لحظة.

---

## 3. أخطاء منخفضة (Severity: Low) 💡

### 3.1 دوال كشف الأجهزة والمنصات بدائية
*   **الموقع:** `app/Http/Controllers/Public/CardViewController.php` (دوال `detectDeviceType` و `detectBrowser`)
*   **الوصف:** تعتمد الدوال على `preg_match` بسيط للـ User-Agent مما قد يعطي نتائج غير دقيقة للأجهزة الحديثة.
*   **الحل المقترح:** استخدام مكتبة متخصصة مثل `jenssegers/agent`.

### 3.2 وجود ملفات زائدة (Redundant Files)
*   **الموقع:** `resources/views/welcome.blade.php`.
*   **الوصف:** ما زال ملف الترحيب الافتراضي لـ Laravel موجوداً ومعدلاً بشكل طفيف، رغم أن الصفحة الرئيسية الفعلية هي `resources/views/public/home.blade.php`.
*   **الأثر:** ارتباك للمطورين المستقبليين وزيادة حجم المشروع بلا داعٍ.

---

## 4. إحصائيات الفحص

| نوع المشكلة | العدد |
|-------------|-------|
| أخطاء منطقية (Logic Bugs) | 3 |
| مشاكل أمنية (Security) | 1 |
| عيوب معمارية (Architectural) | 1 |
| ملفات زائدة | 1 |

---
*تم إعداد هذا التقرير بناءً على فحص مباشر للكود المصدري.*
