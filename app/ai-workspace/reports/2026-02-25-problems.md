# تقرير المشاكل التقنية - معروف (Maroof ID)
# التاريخ: 2026-02-25

هذا التقرير يوضح المشاكل البرمجية والعيوب التي تم اكتشافها أثناء الفحص الشامل للمشروع.

---

## 1. مشاكل خطيرة (حرجة) 🚨

### 1.1 فقدان بيانات الروابط الاجتماعية (Data Loss)
* **الموقع:** `app/Http/Controllers/Customer/CardController.php` و `app/Http/Controllers/Customer/CardBuilderController.php`.
* **الوصف:** عند تحديث أي بطاقة، يقوم النظام بحذف جميع الروابط الاجتماعية المرتبطة بها ثم إعادة إنشائها من جديد.
* **الأثر:** يؤدي هذا إلى تصفير عداد النقرات (`clicks_count`) لجميع الروابط في كل مرة يتم فيها تعديل البطاقة، مما يفقد المستخدم إحصائيات هامة جداً.
* **الحل المقترح:** استخدام `updateOrCreate` أو المزامنة (`sync`) بدلاً من الحذف والإنشاء الكامل.

### 1.2 عدم تطابق نموذج التسجيل مع قاعدة البيانات
* **الموقع:** `app/Http/Controllers/Auth/RegisterController.php`.
* **الوصف:** نموذج التسجيل يطلب ويتحقق من حقل `phone` (رقم الجوال)، ولكن عمود `phone` غير موجود في جدول `users` في قاعدة البيانات، وغير موجود أيضاً في مصفوفة `$fillable` في مودل `User`.
* **الأثر:** يتم تجاهل رقم جوال المستخدم عند التسجيل ولا يتم حفظه، مما يصعب التواصل مع العملاء لاحقاً.

---

## 2. مشاكل أمنية 🔒

### 2.1 تخزين كلمات مرور البطاقات بنص صريح
* **الموقع:** مودل `Card.php`.
* **الوصف:** ميزة حماية البطاقة بكلمة مرور تقوم بتخزين الكلمة بنص صريح (Plain Text) في قاعدة البيانات وتتم المقارنة باستخدام المشغل `===`.
* **الأثر:** خطر أمني في حال وصول أي شخص لقاعدة البيانات، حيث يمكن رؤية كلمات مرور جميع المستخدمين لبطاقاتهم.
* **الحل المقترح:** تشفير كلمات المرور باستخدام `Hash::make` عند الحفظ والمقارنة باستخدام `Hash::check`.

---

## 3. ملاحظات برمجية (Code Quality) 💻

### 3.1 الاعتماد على خدمات خارجية دون الحاجة
* **الموقع:** `CardController.php` (دالة `downloadQR`).
* **الوصف:** يتم استخدام رابط خارجي (`api.qrserver.com`) لتوليد رموز QR، رغم وجود خدمة محلية قوية (`QrCodeService.php`) ومكتبة مثبتة بالفعل في المشروع.
* **الأثر:** بطء محتمل في توليد الرموز، والاعتماد على خدمة طرف ثالث قد تتوقف في أي لحظة.

### 3.2 وجود ملفات زائدة (Redundant Files)
* **الموقع:** `resources/views/welcome.blade.php`.
* **الوصف:** ما زال ملف الترحيب الافتراضي لـ Laravel موجوداً ومعدلاً بشكل طفيف، رغم أن الصفحة الرئيسية الفعلية هي `resources/views/public/home.blade.php`.
* **الأثر:** ارتباك للمطورين المستقبليين وزيادة حجم المشروع بلا داعٍ.

---

## 4. إحصائيات الفحص

| نوع المشكلة | العدد |
|-------------|-------|
| أخطاء منطقية (Logic Bugs) | 2 |
| مشاكل أمنية (Security) | 1 |
| عيوب معمارية (Architectural) | 1 |
| ملفات زائدة | 1 |

---
*تم إعداد هذا التقرير بواسطة مساعد الذكاء الاصطناعي (Jules)*
