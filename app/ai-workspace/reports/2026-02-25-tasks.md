# تقرير المهام والنواقص - معروف (Maroof ID)
# التاريخ: 2026-02-25

هذا التقرير يحدد المهام البرمجية المتبقية والميزات التي تحتاج إلى إكمال قبل الإطلاق الرسمي.

---

## 1. مهام التكامل (Integrations) 🔗

### 1.1 تفعيل بوابة الدفع (Tap.sa / Stripe)
* **الوضع الحالي:** الخدمات موجودة كـ "Mock" أو Placeholder ولا تقوم بعمليات دفع حقيقية.
* **المطلوب:** دمج الـ API Keys الحقيقية وبرمجة الـ Webhooks للتحقق من نجاح العمليات وتحديث حالة الطلبات تلقائياً.

### 1.2 تفعيل نظام الرسائل القصيرة (SMS)
* **الوضع الحالي:** كلاس `SmsService.php` فارغ من المنطق البرمجي الحقيقي.
* **المطلوب:** ربط خدمة Twilio أو أي مزود محلي لإرسال رسائل التحقق (OTP) وتنبيهات الطلبات.

---

## 2. مهام التحسين (Improvements) ✨

### 2.1 استخدام مكتبة QR المحلية
* **المطلوب:** تعديل `CardController` ليستخدم `QrCodeService` المدمجة بدلاً من الطلبات الخارجية لتعزيز السرعة والاستقرار.

### 2.2 تحسين معالجة بيانات الروابط الاجتماعية
* **المطلوب:** إعادة كتابة منطق حفظ الروابط ليكون ذكياً (Update existing, delete removed) للحفاظ على إحصائيات النقرات.

### 2.3 تحديث قاعدة بيانات المستخدمين
* **المطلوب:** إضافة عمود `phone` لجدول `users` وتحديث المودل ليقبل الحفظ، ليتوافق مع شاشة التسجيل.

---

## 3. مهام ناقصة (Unfinished Features) 🛠️

### 3.1 إكمال نموذج الاتصال (Contact Form)
* **الموقع:** `HomeController.php` دالة `submitContact`.
* **المطلوب:** برمجة إرسال إيميل للإدارة عند استلام رسالة جديدة، أو حفظ الرسالة في قاعدة البيانات ليتسنى للإدارة الرد عليها من لوحة التحكم.

### 3.2 إكمال رحلة المستخدم (User Journeys)
* **المطلوب:** كتابة ملفات `partner-journey.md` و `designer-journey.md` كما هو مذكور في المستند الأساسي للمشروع، حيث أنها لا تزال مفقودة.

---

## 4. قائمة التحقق للإطلاق (Launch Checklist) ✅

- [ ] إضافة API Keys للبيئة الإنتاجية (`.env`).
- [ ] إجراء اختبار كامل لعملية الشراء (End-to-End).
- [ ] التأكد من عمل نظام الـ Queues لإرسال الإيميلات.
- [ ] تنظيف الملفات الزائدة (مثل `welcome.blade.php`).
- [ ] إعداد نظام النسخ الاحتياطي (Backups).

---
*تم إعداد هذا التقرير بواسطة مساعد الذكاء الاصطناعي (Jules)*
