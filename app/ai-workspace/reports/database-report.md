# تقرير حالة قاعدة البيانات (Database Report)
# التاريخ: 2026-02-25

يوضح هذا التقرير هيكلية قاعدة البيانات، النواقص في الأعمدة، وكفاءة الفهارس.

---

## 1. مشاكل السكيما (Schema Issues) 📊

### 1.1 نقص أعمدة أساسية
*   **جدول `users`:** يفتقر لعمود `phone` رغم أهميته في نموذج التسجيل وعملية التواصل مع العملاء.
*   **جدول `card_social_links`:** يحتاج لربط أقوى مع المودل لمنع فقدان البيانات أثناء التحديث (كما ذُكر في تقرير المشاكل).

### 1.2 نوع البيانات وتخزينها
*   **كلمات مرور البطاقات:** تُخزن في جدول `cards` كنص صريح (Plain Text). يجب تحويل نوع العمود أو طريقة المعالجة لدعم التشفير.

---

## 2. الفهارس والأداء (Indexing & Performance) 🚀

*   تم إضافة فهارس أداء مؤخراً في ملف `2026_02_23_141456_add_performance_indexes.php` وهي تغطي أغلب الاستعلامات الشائعة للبطاقات والمشاهدات.
*   **ملاحظة:** جدول `transactions` قد يحتاج لفهرس على عمود `transaction_id` لسرعة البحث عند استخدام Webhooks الخاصة بالدفع.

---

## 3. اتساق الهجرات (Migrations Consistency) 📂

*   يوجد تداخل في توقيت بعض الهجرات (Migrations) التي تحمل نفس الطابع الزمني، مثل `create_orders_table` و `create_template_categories_table`. هذا لا يسبب مشكلة تقنية ولكنه غير مفضل تنظيمياً.
*   وجود ملف `2026_02_12_000012_add_foreign_keys_to_existing_tables.php` كملف منفصل لإضافة المفاتيح الأجنبية هو ممارسة جيدة لتجنب مشاكل الترتيب عند إنشاء الجداول.

---

## 4. التوصيات 📝

1.  **إضافة عمود الهاتف:** تشغيل Migration جديد لإضافة `phone` لجدول `users`.
2.  **تأمين كلمات المرور:** تحديث الكود والمودل لتشفير حقل `password` في جدول `cards`.
3.  **تدقيق العلاقات:** التأكد من تفعيل `On Delete Cascade` في جميع الجداول المرتبطة بالبطاقات لضمان نظافة قاعدة البيانات عند الحذف.

---
*تم فحص قاعدة البيانات لضمان سلامة البيانات وكفاءة الأداء.*
