# قائمة المهام والتوصيات التقنية (Technical Tasks & TODOs)
# التاريخ: 2026-02-25

يسرد هذا التقرير المهام المطلوبة لتحسين المشروع ووصوله لمرحلة الجاهزية الكاملة.

---

## 1. مهام عاجلة (Urgent Tasks) 🔴

1.  **قاعدة البيانات:** إضافة حقل `wallet_balance` لجدول `users` لتفعيل الدفع من الرصيد.
2.  **الأمان:** دمج مكتبة `HTML Purifier` لتنقية القوالب المرفوعة من المصممين لمنع هجمات XSS.
3.  **المالية:** إكمال برمجة بوابة Stripe أو إيقاف خيار الدفع بها حتى يتم ربطها حقيقياً.

---

## 2. مهام التحسين (Refactoring & Optimization) 🟡

1.  **توليد الـ QR:** استبدال رابط الـ API الخارجي بمكتبة `simplesoftwareio/simple-qrcode` لتوليد الرموز محلياً.
2.  **الهوية البصرية:** توحيد الألوان بين لوحة التحكم والواجهة الرئيسية (تعديل ملف `tailwind.config.js`).
3.  **إعادة هيكلة الـ API:** تحديث `Api/CardController` ليستخدم الـ `CardPolicy` بدلاً من التحقق اليدوي.
4.  **منطق النسخ:** توحيد دالة `duplicate` في `CardService` واستدعاؤها من الـ Controller.

---

## 3. ميزات قيد التطوير (Features in Progress) 🔵

1.  **نظام الإشعارات:** ربط `SmsService` بمزود خدمة حقيقي واختبار إرسال الـ OTP.
2.  **استوديو التصميم:** تقليل الاعتماد على Inline JS ونقل الكود لملفات خارجية منظمة.
3.  **التقارير الإدارية:** إضافة رسوم بيانية ملخصة في لوحة الإدارة للطلبات والأرباح.

---

## 4. قائمة التحقق النهائية (Final Checklist) ✅

- [ ] فحص سجل الأخطاء (Logs) والتأكد من عدم وجود Runtime Exceptions.
- [ ] التأكد من عمل نظام الـ Queues لإرسال البريد الإلكتروني.
- [ ] فحص توافق الواجهة مع الهواتف المحمولة في جميع الأقسام.
- [ ] تنظيف الكود من أي `dd()` أو `Log::debug()` زائدة.

---
*هذه المهام تمثل خارطة الطريق لتحسين استقرار وأمان المشروع.*
